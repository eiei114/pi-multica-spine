# R-MNT-22..24 closeout (v0.7.5)

## R-MNT-22 — Sandbox full closeout

- `workflow-sandbox-rehearsal.mjs` supports `--full-closeout` (preflight → apply → campaign → human-review).
- Live path uses `FULL_LIVE_CAMPAIGN_STAGE_CYCLES` (80) to reach `final_package` before review.

## R-MNT-23 — Production rehearsal

- `workflow-production-rehearsal.mjs` validates Maintenance start/campaign/human-review wiring offline.
- `--execute` runs live multica ops with `productionAllowed=false`.

## R-MNT-24 — Sandbox line floors

- `COVERAGE_SANDBOX_LINE_FLOORS` in `coverage-gate.mjs` for campaign, fixtures, autopilot, human-review.
