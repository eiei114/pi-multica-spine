# R-MNT-16..18 closeout (v0.7.3)

## R-MNT-16 — Full offline campaign

- `examples/workflow-campaign-walkthrough/run-walkthrough.mjs` runs `runCanaryCampaign` with `FULL_OFFLINE_CAMPAIGN_STAGE_CYCLES` (80) through `final_package`.
- Removed manual `prepareWalkthroughHumanReviewLedger` seeding; human review requires natural campaign completion.

## R-MNT-17 — Sandbox checklist automation

- `scripts/workflow-sandbox-checklist.mjs` validates dist/lib, sandbox dry-run plan, blocked project guard, and daemon marker cleanup.
- `--live` mode additionally probes `multica` CLI auth before real sandbox ops.

## R-MNT-18 — Template periodic re-sync

- `scripts/template-resync-check.mjs` encodes pi-extension-template@0.1.6 guardrails (CI/publish majors, DOT-881 no-push, curl pre-check, package metadata).
- `npm run check:template` runs in CI.
