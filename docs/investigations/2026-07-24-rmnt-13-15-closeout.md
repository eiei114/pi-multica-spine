# R-MNT-13..15 closeout (v0.7.2)

## R-MNT-13 — Coverage hotspots

- `scripts/coverage-gate.mjs` enforces `COVERAGE_HOTSPOTS` per-file floors on critical lib modules.
- `COVERAGE_DENYLIST` excludes integration-heavy campaign/fixture files from hotspot enforcement.

## R-MNT-14 — Publish registry HTTP pre-check

- `.github/workflows/publish.yml` queries `https://registry.npmjs.org/<pkg>/<version>` via `curl` **before** `setup-node` OIDC.
- Avoids authenticated `npm view` false 404 after OIDC (pi-extension-template pattern).

## R-MNT-15 — Walkthrough human review

- `examples/workflow-campaign-walkthrough/` seeds `final_package` + runs `completeHumanFinalReview` offline after sample campaign stages.
- Documented as rehearsal, not a substitute for live Multica closeout.
