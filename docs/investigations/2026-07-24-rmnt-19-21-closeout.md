# R-MNT-19..21 closeout (v0.7.4)

## R-MNT-19 — Sandbox rehearsal automation

- `scripts/workflow-sandbox-rehearsal.mjs` runs preflight → apply → campaign.
- Offline mode (CI) validates plan wiring; `--execute` runs live multica apply+campaign with bounded `--max-stage-cycles`.

## R-MNT-20 — Sandbox branch coverage floors

- `COVERAGE_SANDBOX_BRANCH_FLOORS` in `coverage-gate.mjs` for campaign, fixtures, autopilot, human-review modules.

## R-MNT-21 — Template peer bump check

- `evaluatePiPeerVersions` ensures pi peer packages stay on template `0.80.x` baseline.
